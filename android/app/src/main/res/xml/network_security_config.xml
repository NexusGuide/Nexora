<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTPS only. The app carries access tokens and config URIs; on the networks
  this product exists to serve, plaintext is not a reasonable default.

  The debug overlay permits cleartext to the emulator loopback alias only, so a
  developer can run the backend locally without weakening the release build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
